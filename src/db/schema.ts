import { sql } from 'drizzle-orm';import { boolean,index,integer,jsonb,pgTable,primaryKey,text,timestamp,uniqueIndex,uuid } from 'drizzle-orm/pg-core';
const now=()=>timestamp('created_at',{withTimezone:true}).defaultNow().notNull();
export const players=pgTable('players',{id:uuid('id').primaryKey().defaultRandom(),steamId:text('steam_id').unique(),displayName:text('display_name').notNull(),avatarUrl:text('avatar_url'),status:text('status').notNull().default('active'),rating:integer('rating').notNull().default(1000),trustScore:integer('trust_score').notNull().default(100),region:text('region').notNull().default('EU West'),isBot:boolean('is_bot').notNull().default(false),createdAt:now(),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull(),lastSeenAt:timestamp('last_seen_at',{withTimezone:true})},t=>({statusIdx:index('players_status_idx').on(t.status),botIdx:index('players_is_bot_idx').on(t.isBot)}));
export const playerRoles=pgTable('player_roles',{playerId:uuid('player_id').references(()=>players.id,{onDelete:'cascade'}).notNull(),role:text('role').notNull(),priority:integer('priority').notNull().default(1)},t=>({pk:primaryKey({columns:[t.playerId,t.role]})}));
export const queueEntries=pgTable('queue_entries',{id:uuid('id').primaryKey().defaultRandom(),playerId:uuid('player_id').references(()=>players.id,{onDelete:'cascade'}).notNull(),region:text('region').notNull(),primaryRole:text('primary_role').notNull(),secondaryRole:text('secondary_role').notNull(),ratingSnapshot:integer('rating_snapshot').notNull(),trustScoreSnapshot:integer('trust_score_snapshot').notNull(),status:text('status').notNull().default('waiting'),joinedAt:timestamp('joined_at',{withTimezone:true}).defaultNow().notNull(),matchedAt:timestamp('matched_at',{withTimezone:true}),cancelledAt:timestamp('cancelled_at',{withTimezone:true}),version:integer('version').notNull().default(1)},t=>({active:uniqueIndex('queue_one_active_per_player').on(t.playerId).where(sql`status = 'waiting'`),waiting:index('queue_waiting_idx').on(t.region,t.status,t.joinedAt)}));
export const matches=pgTable('matches',{id:uuid('id').primaryKey().defaultRandom(),roomCode:text('room_code').notNull().unique(),region:text('region').notNull(),status:text('status').notNull().default('accepting'),acceptDeadline:timestamp('accept_deadline',{withTimezone:true}).notNull(),startedAt:timestamp('started_at',{withTimezone:true}),completedAt:timestamp('completed_at',{withTimezone:true}),cancelledAt:timestamp('cancelled_at',{withTimezone:true}),winner:text('winner'),radiantScore:integer('radiant_score').notNull().default(0),direScore:integer('dire_score').notNull().default(0),createdAt:now(),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()},t=>({status:index('matches_status_idx').on(t.status)}));
export const matchPlayers=pgTable('match_players',{matchId:uuid('match_id').references(()=>matches.id,{onDelete:'cascade'}).notNull(),playerId:uuid('player_id').references(()=>players.id,{onDelete:'cascade'}).notNull(),team:text('team').notNull(),role:text('role').notNull(),acceptStatus:text('accept_status').notNull().default('pending'),ratingBefore:integer('rating_before').notNull(),ratingAfter:integer('rating_after'),trustScoreBefore:integer('trust_score_before').notNull(),trustScoreAfter:integer('trust_score_after'),joinedAt:timestamp('joined_at',{withTimezone:true}).defaultNow().notNull(),acceptedAt:timestamp('accepted_at',{withTimezone:true})},t=>({pk:primaryKey({columns:[t.matchId,t.playerId]}),player:index('match_players_player_idx').on(t.playerId)}));
export const ratingEvents=pgTable('rating_events',{id:uuid('id').primaryKey().defaultRandom(),playerId:uuid('player_id').references(()=>players.id).notNull(),matchId:uuid('match_id').references(()=>matches.id),reason:text('reason').notNull(),delta:integer('delta').notNull(),valueBefore:integer('value_before').notNull(),valueAfter:integer('value_after').notNull(),createdAt:now()});
export const trustEvents=pgTable('trust_events',{id:uuid('id').primaryKey().defaultRandom(),playerId:uuid('player_id').references(()=>players.id).notNull(),matchId:uuid('match_id').references(()=>matches.id),reason:text('reason').notNull(),delta:integer('delta').notNull(),valueBefore:integer('value_before').notNull(),valueAfter:integer('value_after').notNull(),createdAt:now()});
export const runtimeConfig=pgTable('runtime_config',{key:text('key').primaryKey(),value:jsonb('value').notNull(),description:text('description').notNull(),isPublic:boolean('is_public').notNull().default(false),updatedBy:text('updated_by').notNull().default('system'),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()});
export const featureFlags=pgTable('feature_flags',{key:text('key').primaryKey(),enabled:boolean('enabled').notNull().default(false),description:text('description').notNull(),updatedBy:text('updated_by').notNull().default('system'),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()});
export const patches=pgTable('patches',{id:uuid('id').primaryKey().defaultRandom(),version:text('version').notNull().unique(),title:text('title').notNull(),summary:text('summary').notNull(),changelog:jsonb('changelog').notNull(),status:text('status').notNull().default('draft'),publishedAt:timestamp('published_at',{withTimezone:true}),createdAt:now(),updatedAt:timestamp('updated_at',{withTimezone:true}).defaultNow().notNull()});
export const sanctions=pgTable('sanctions',{id:uuid('id').primaryKey().defaultRandom(),playerId:uuid('player_id').references(()=>players.id).notNull(),type:text('type').notNull(),reason:text('reason').notNull(),expiresAt:timestamp('expires_at',{withTimezone:true}),createdBy:text('created_by').notNull(),createdAt:now(),revokedAt:timestamp('revoked_at',{withTimezone:true})},t=>({active:index('sanctions_active_idx').on(t.playerId,t.revokedAt)}));
export const auditLogs=pgTable('audit_logs',{id:uuid('id').primaryKey().defaultRandom(),actorType:text('actor_type').notNull(),actorId:text('actor_id').notNull(),action:text('action').notNull(),entityType:text('entity_type').notNull(),entityId:text('entity_id').notNull(),oldValue:jsonb('old_value'),newValue:jsonb('new_value'),ipAddress:text('ip_address'),createdAt:now()},t=>({created:index('audit_created_idx').on(t.createdAt)}));
